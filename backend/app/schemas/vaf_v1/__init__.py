"""Pydantic schemas for the VAF v1 service contract (PAF integration).

This package mirrors the public-facing TypeScript shapes defined in the
PAF-Shadow VAF integration spec. Field names use camelCase exactly as the
spec dictates so JSON payloads round-trip without translation layers.
"""
