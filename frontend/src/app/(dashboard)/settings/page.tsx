import PageStub from "@/components/ui/PageStub";

export default function SettingsPage() {
  return <PageStub moduleName="Settings" description="Workspace settings, user preferences, and configuration." />;
}
