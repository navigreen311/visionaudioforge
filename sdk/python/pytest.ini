[pytest]
# auto mode so async tests and async fixtures work without per-item decorators.
# Without it, pytest-asyncio's strict mode silently errors on async fixtures.
asyncio_mode = auto
testpaths = tests
